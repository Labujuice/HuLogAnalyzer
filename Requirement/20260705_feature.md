# 專案需求與設計規格書：進階日誌分析與模組化客製運算功能 (2026-07-05 修正版)

## 1. 專案背景與擴充目標 (Project Overview)

本需求文件為純前端 PX4 ULog 分析儀表板之進階後處理與分析功能的規格修正版。基於先前討論與使用者反饋，本次功能擴充包含五大分析模組、自訂時序對齊、側邊欄快捷繪圖區（按需計算）以及自定義運算導入（模組化配置），藉以支援深度飛行診斷。

---

## 2. 核心分析功能設計 (Core Analysis Features)

### 2.1 振動量頻譜分析 (Vibration FFT Analysis - Freq vs. Amplitude)
* **呈現圖表：** 頻率 (Frequency, Hz) vs. 幅值 (Amplitude)。
* **多感測器獨立顯示：** 
  * 加速度計 (Accelerometer)：`sensor_accel:0`（三軸 X, Y, Z 各自呈線）。
  * 陀螺儀 (Gyroscope)：`sensor_gyro:0`（三軸 X, Y, Z 各自呈線）。
* **計算公式：**
  * 去直流分量 (De-trending)：$x_i' = x_i - \bar{x}$。
  * 加漢寧窗 (Hanning Window) 降低頻譜洩漏：$x_i'' = x_i' \cdot 0.5 \left[1 - \cos\left(\frac{2\pi i}{N-1}\right)\right]$。
  * 離散傅立葉轉換 (FFT) 後，振幅計算公式為：
    $$\text{Amplitude}(f) = \frac{2 \cdot |X(f)|}{N} \quad (f > 0)$$
    $$\text{Amplitude}(0) = \frac{|X(0)|}{N}$$
* **性能優化：** 
  * 採**按需計算 (Compute-on-demand)** 策略。只有在建立或選取該分析面板，且按下計算時，Worker 才執行 FFT 運算。
  * 支援時間框選 (Time selection) 解析，自動載入框選範圍內數據進行 FFT（點數補零或截斷至 $2^n$ 點，以利 Radix-2 演算法）。

### 2.2 多層級控制器響應與同軸對比分析 (Multi-level PID Controller Response & Setpoint/Feedback Tracking)
比對「期望指令 (Setpoint)」與「實測回饋 (Actual/Estimated)」，涵蓋以下四個主要控制環路：

| 控制層級 | 期望值資料源 (Setpoint Topic & Fields) | 實測值資料源 (Actual Topic & Fields) |
| :--- | :--- | :--- |
| **角速度 (Rate)** | `vehicle_rates_setpoint`<br>• `roll`, `pitch`, `yaw` | `vehicle_angular_velocity`<br>• `xyz[0]`, `xyz[1]`, `xyz[2]` |
| **姿態角 (Attitude)** | `vehicle_attitude_setpoint`<br>• 尤拉角轉換或 `q_d[4]` | `vehicle_attitude`<br>• 四元數 `q[4]` 轉 Roll, Pitch, Yaw |
| **速度 (Velocity)** | `vehicle_local_position_setpoint`<br>• `vx`, `vy`, `vz` | `vehicle_local_position`<br>• `vx`, `vy`, `vz` |
| **位置 (Position)** | `vehicle_local_position_setpoint`<br>• `x`, `y`, `z` | `vehicle_local_position`<br>• `x`, `y`, `z` |

本模組拆分為兩大呈現方式，皆支援對齊與時域修正：

1. **同軸時序追隨對比圖 (Setpoint & Feedback Synced Plotting)**：
   * **分軸處理 (Axis Splitting)**：為了避免將多軸數據混在同一圖表導致混亂，圖表必須按物理軸向（Roll、Pitch、Yaw，或 X、Y、Z 軸）進行**分軸獨立繪製**。
   * **呈現方式**：在同一個畫框內，分軸顯示期望值（Setpoint，如綠色虛線）與實際值（Feedback，如紅色實線）的對比。使用者可以透過分頁或子圖表，分開觀察 Roll、Pitch、Yaw 的跟隨情況。
2. **控制器響應評估 (Controller Response Analysis & Metrics)**：
   * **分軸評估 (Axis-specific Evaluation)**：專注於量化追隨性能，並依物理軸向（Roll、Pitch、Yaw，或 X、Y、Z 軸）分別計算與繪製各自的「偏差誤差 (Error Offset)」時序曲線。
   * **分軸輸出指標**：對各軸獨立計算並輸出以下指標：
     * **分軸均方根誤差 (RMSE)**：評估該軸向的絕對追隨偏差。
     * **分軸皮爾森相關係數 (Pearson R)**：評估該軸向動態響應的相位與趨勢一致性。
     * **分軸相位延遲 (Phase Lag)**：估算該軸向毫秒級的控制反應時間差。

* **數據對齊與時鐘抖動校正：**
  1. **線性插值對齊 (Time Interpolation)**：以實測值時間戳 $t_{\text{ref}}$ 為基準，將期望值插值至該時間點。
  2. **延遲校正與時間對齊 (Jitter & Skew Correction)**：
     * 對於具有時間偏置 (Time offset) 的主題，提供一個簡單的互相關 (Cross-correlation) 延遲偵測，求出使兩序列相似度最高的 $\Delta t$，在插值前先對 Setpoint 時間戳進行平移補償：
       $$t_{\text{setpoint}}' = t_{\text{setpoint}} - \Delta t$$

### 2.3 馬達輸出平衡與電調分析 (Motor & ESC Analysis)
* **馬達軸數自適應**：動態檢測 ULog 內的 `actuator_outputs` 與 `esc_status` Channel 數量（自動適配 4, 6, 8 等多旋翼佈局）。
* **分析輸出**：
  1. **ESC Telemetry 轉速回饋**：若存在 `esc_status`，繪製各馬達 RPM 時序圖。
  2. **Actuator Outputs 命令輸出**：繪製 `actuator_outputs.output[i]` 的時序命令圖（PWM 脈寬或 DShot 數值）。
  3. **Hover 平衡偏離率柱狀圖**：在懸停狀態下，計算各馬達相對於總均值的離差 $\text{Deviation}_j$，用以分析硬體結構是否歪斜或槳葉受損。

### 2.4 磁場強度與航向角分析 (Magnetic Field & Heading Analysis)
1. **磁場模長分析 (Magnetic Vector Norm)**：
   * 讀取磁力計數據（如 `vehicle_magnetometer` 欄位 `magnetometer_ga[3]` 或是 `sensor_mag`）。
   * 計算三軸合成磁場強度模長（單位：Gauss 或 Tesla）：
     $$\text{Mag Norm} = \sqrt{B_x^2 + B_y^2 + B_z^2}$$
   * 用於檢測大電流產生的電磁干擾 (EMI)。
2. **航向對比分析 (Heading Comparison & Alignment)**：
   * **EKF 融合航向 (EKF Estimated Yaw)**：從 `vehicle_attitude` 的四元數 $q$ 計算主要 Yaw 角：
     $$\text{Yaw}_{\text{est}} = \operatorname{atan2}\left(2(q_0 q_3 + q_1 q_2), 1 - 2(q_2^2 + q_3^2)\right)$$
   * **EKF GSF 航向 (EKF GSF Yaw)**：讀取 EKF GSF (Gaussian Sum Filter) 航向估測器 Topic `yaw_estimator_status`（例如 `yaw` 欄位）。當磁力計受干擾或失效時，GSF 利用 GPS 與 IMU 獨立估算航向，將此與 EKF 融合航向重疊對比，能有效診斷指南針異常與初始化偏置。
   * **測量與外部航向 (Measured Yaw & GPS COG)**：
     * 比對 GPS 航向角 `sensor_gps.cog_rad` (Course Over Ground)。
     * 或利用傾角補償後的磁力計讀值進行磁航向估算。
   * **多源航向對比圖**：在一張圖表中同時繪製 EKF Yaw、EKF GSF Yaw、Magnetometer Yaw、GPS COG，進行多源對比。

### 2.5 飛行狀態與模式分析 (Flight Status & Modes Analysis)
為幫助使用者快速掌握飛行狀態，降低手動過濾 Topic 的時間，本模組彙整並解碼以下關鍵指標：

1. **目前飛行模式與解鎖狀態 (Flight Mode & Arming State)**：
   * **Topic 與欄位**：`vehicle_status` (欄位 `nav_state` 與 `arming_state`)。
   * **功能**：讀取數值並將其映射為人類可讀字串（如 `POSCTL`, `AUTO_MISSION`, `ARMED`），並在圖表中以色帶或標籤標記模式變更的時間點。
2. **遙控器搖桿輸入與介入 (Manual Stick Input & Pilot Intervention)**：
   * **Topic 與欄位**：`manual_control_setpoint` (欄位 `x` (Pitch), `y` (Roll), `z` (Throttle), `r` (Yaw))。
   * **功能**：觀察操縱桿 $[-1, 1]$ 區間變化。當搖桿有明顯動作時，表示「使用者介入操作」，可用於輔助判斷姿態突變是來自人為控制還是外界干擾。
3. **系統故障與估算器錯誤 (System Faults & Estimator Errors)**：
   * **Topic 與欄位**：`estimator_status` (如 `control_mode_flags`, `gps_check_fail_flags`, `filter_fault_flags` 等位元遮罩) 以及系統 `L` 文字日誌。
   * **功能**：解碼估算器錯誤旗標，在時序圖上標記 EKF 故障（如 magnetometer check fail、velocity check fail 等）或感測器失效時間點。
4. **安全防護觸發狀態 (Failsafe Triggers)**：
   * **Topic 與欄位**：`vehicle_status` (欄位 `failsafe`, `rc_signal_lost`) 與 `battery_status` (欄位 `warning`)。
   * **功能**：直觀顯示是否觸發 Failsafe（如低電量返航、遙控訊號中斷失控保護）。

---

## 3. 側邊欄版面與交互設計 (UI/UX layout)

側邊欄 (Sidebar) 改為上下兩區塊設計，強化快速分析的便利性：

```
+------------------------------------+
|                                    |
|         Topics / Field Tree        |
|     (原始數據樹狀圖，支援關鍵字搜尋)     |
|                                    |
+------------------------------------+
| ====== 繪圖快捷區 (Quick Plots) ===== |
|   [📈 振動量分析 (FFT)]             |
|   [🎯 控制器追隨響應]                |
|   [⚡ 馬達與電調回饋]                |
|   [🧲 磁場與航向角分析]              |
|   [🎛️ 飛行狀態與模式]                |
+------------------------------------+
```

### 3.1 繪圖快捷區 (Quick Plot Presets) 運作流程
1. **點選/拖曳觸發**：使用者可以將快捷區的分析套餐拖曳至空的 dashboard 畫框（或點擊該套餐）。
2. **延遲計算 (Deferred Computation)**：
   * 拖曳落入畫框之前，系統**完全不進行** FFT、插值對齊或 Norm 計算。
   * 拖入落定後，對應畫框轉為 Loading 狀態，主執行緒發送計算請求至 Web Worker。
   * Worker 計算完成後，回傳 TypedArrays（如 FFT 頻譜數值），圖表即時繪製出來。此設計保證了無效計算零佔用，前端絕不卡頓。

---

## 4. 模組化客製運算管理 (Modular Custom Calculations)

為了預留未來使用者導入自訂計算結構，系統設計了一套 JSON 格式的客製運算描述子 (Calculation Config Descriptor)。使用者可以透過 UI 上傳自訂的配置檔來宣告新的計算指標，系統將自動解析並新增至「自訂快捷區」。

### 4.1 自訂配置 JSON Schema 範例
```json
{
  "id": "custom-battery-power",
  "name": "電池瞬時功率計算",
  "description": "計算電壓與電流之乘積，評估瞬時耗能",
  "targetChart": {
    "title": "電池功率 (W)",
    "yLabel": "Power (W)",
    "color": "#fbbf24"
  },
  "dependencies": {
    "primary": {
      "topic": "battery_status",
      "fields": ["voltage_v", "current_a"]
    }
  },
  "operations": [
    {
      "type": "multiply",
      "inputs": [
        "battery_status.voltage_v",
        "battery_status.current_a"
      ],
      "output": "power_w"
    }
  ]
}
```

### 4.2 數學與信號運算子規範 (Math & Signal Operators Specification)

客製運算描述子支援以下兩種宣告方式：**「結構化運算子鏈 (Structured Operator Chain)」** 與 **「算術公式解析器 (Expression Parser)」**。

#### 4.2.1 結構化運算子 (Structured Operators)
每個運算步驟定義為一個物件，包含 `type` (運算型別)、`inputs` (輸入欄位或數值常數)、`output` (輸出暫存變數名稱) 以及選填的 `params` (特定參數)：

1. **基本算術運算子 (`add` | `subtract` | `multiply` | `divide` | `power`)**：
   * **語法**：
     ```json
     {
       "type": "add",
       "inputs": ["sensor_accel.x", 9.80665], 
       "output": "accel_x_g_adjusted"
     }
     ```
     *(註：`inputs` 的陣列元素可以是 Topic 的欄位字串，也可以是常數數字。若為兩變數運算，長度需為 2；若為多元加法/乘法，長度可大於 2。)*
2. **向量模長運算子 (`norm`)**：
   * 計算輸入陣列的幾何歐幾里得模長，適用於三軸合成強度。
   * **語法**：
     ```json
     {
       "type": "norm",
       "inputs": ["mag.x", "mag.y", "mag.z"],
       "output": "mag_norm"
     }
     ```
3. **時間序列插值對齊 (`interpolate`)**：
   * 將變數插值對齊到主參考序列（`reference`）的時間軸上，供後續兩 Topic 交叉運算使用。
   * **語法**：
     ```json
     {
       "type": "interpolate",
       "inputs": ["vehicle_rates_setpoint.roll"],
       "reference": "vehicle_angular_velocity.xyz[0]",
       "output": "aligned_roll_setpoint"
     }
     ```
4. **信號處理運算子 (`fft` | `lowpass`)**：
   * **`fft`**：將時域欄位轉換為頻域幅值。
     ```json
     {
       "type": "fft",
       "inputs": ["sensor_accel.x"],
       "params": { "window": "hanning", "detrend": true },
       "output": "fft_amplitude_x"
     }
     ```
   * **`lowpass`**：一階數位濾波器。
     ```json
     {
       "type": "lowpass",
       "inputs": ["sensor_gyro.x"],
       "params": { "cutoff_hz": 20.0 },
       "output": "filtered_gyro_x"
     }
     ```

#### 4.2.2 算術公式解析器 (Expression Parser)
若需要進行複雜的多項式代數運算，結構化寫法會過於冗長。此時可使用 `expression` 類型，由前端公式解析器執行動態求解：

* **語法**：
  ```json
  {
    "type": "expression",
    "formula": "sqrt(mag_x^2 + mag_y^2 + mag_z^2) - offset",
    "variables": {
      "mag_x": "vehicle_magnetometer.magnetometer_ga[0]",
      "mag_y": "vehicle_magnetometer.magnetometer_ga[1]",
      "mag_z": "vehicle_magnetometer.magnetometer_ga[2]",
      "offset": 0.53
    },
    "output": "calibrated_mag_norm"
  }
  ```
* **公式支援語法**：加 `+`、減 `-`、乘 `*`、除 `/`、冪次方 `^`、小括號 `()`，以及常用數學函數 `sqrt()`, `abs()`, `sin()`, `cos()`, `atan2()`, `log()`。

### 4.3 客製化載入與執行管線 (Execution Pipeline)
1. **上傳解析**：使用者上傳配置檔時，前端先驗證 Schema 結構，並掃描 `dependencies` 與 `variables` 宣告的 Topic 是否皆存在於當前載入的 ULog。
2. **Worker 平行運算**：當該客製快捷圖表被拖曳至畫框時，主線程將 JSON 中的 `operations` 發送至 Web Worker。Worker 依據運算鏈，依序執行線性插值對齊、公式運算或 FFT 轉換，保證所有數學運算均在後台執行。
3. **uPlot 接收繪圖**：Worker 將最終產生的 Output 變數數值（以 TypedArray 封裝）與對齊後的 timestamp 回傳，由主線程繪製於圖表。

---

## 5. 系統檔案異動與開發時程 (File Changes & Dev Roadmap)

### 5.1 預計新增與修改檔案
* **`src/parser/fft.ts` (新增)**: 包含基頻、漢寧窗、FFT 幅值計算。
* **`src/parser/mathUtils.ts` (新增)**: 向量 Norm、插值對齊、常規數學運算子與時間戳平滑函數。
* **`src/parser/customCalcParser.ts` (新增)**: 負責讀取 custom JSON，解析運算鏈。
* **`src/components/VibrationPanel.tsx` (新增)**: 獨立呈現 Acc/Gyro 頻譜折線圖。
* **`src/components/PidResponsePanel.tsx` (新增)**: 提供 Rate/Att/Vel/Pos 切換分頁與對位圖表。
* **`src/components/MotorStatusPanel.tsx` (新增)**: 馬達 RPM、輸出與柱狀圖。
* **`src/components/MagneticPanel.tsx` (新增)**: 磁場模長與航向角比較。
* **`src/components/StatusModePanel.tsx` (新增)**: 飛行狀態、遙控器搖桿輸入、系統故障與安全防護時序呈現。
* **`src/components/Sidebar.tsx` (修改)**: 版面切為上下兩區，加入快捷繪圖與 JSON 匯入功能。

### 5.2 實作時程
1. **M1 (2天)**: 基礎數值核心與 FFT 模組實作（含單元測試與時間對齊）。
2. **M2 (2天)**: Web Worker 運算調度及自訂 JSON 解析器框架建置。
3. **M3 (3天)**: 五大進階分析面板（Vibration, PID, Motor, Mag, Status/Mode）UI 與 uPlot 繪圖對接。
4. **M4 (1天)**: 側邊欄快捷操作區及 UI 優化，完成驗收。


***

# user test response
## 20260705 07:06 Review
1. Quick Plot Presets 跟 上面的 topics 之間要可進行範圍移動的bar~ 
2. 開啟新的繪圖匡格時, 請不要將所有的功能跟toolbox的東西都列上去, 維持系統整潔請透過快捷繪圖那邊去增加
3. 看起來 3D/HUD/MAP 可以變成toolbox 內的一個專屬類別, 只有被賦予這個功能才可以被切換, 這樣系統會更簡潔
4. 當只有一個繪圖匡時, 沒辦法透過 右上角的 x 將繪圖、工具清空
5. PID 分析結果我想要看到 step response 的圖片, 可以跟 setpoint vs feedback 這邊做 上下隔開, 上面看原始數據, 下面看 step response, 右邊計算結果與評價請保留
6. 需要特殊計算的 像是 fft 等等的, 這些東西應該是點進去的時候自動觸發計算, 不需要使用者再次點擊計算, 同時計算結果應該就直接暫存了, 不需要每次都重新計算
7. 磁場分析的, 請自動掃描有多少個磁力計, 全部都要計算, 並且 GSF相關訊息沒有被放進去, 僅看到ekf的描述而已... 我也想知道這個場域的真實磁場強度應該要是多少高斯
8. 「Pilot Intervention (Stick Inputs) & Modes」、「Safety Failsafe & RC Link Status」 應該有資料可畫圖, 但你沒畫出來！

### Engineer fix and response
針對 20260705 07:06 Review，以下為所有項目的修正說明與開發反饋：

1. **側邊欄 Topics 與 Presets 移動分欄 Bar**
   * **說明**：在 `Sidebar.tsx` 中實作了拖曳滑鼠事件監聽與 `treeHeight` 狀態。在 Topics Tree 與下方 Quick Plot Presets 之間加入了一條高度調整 Bar（具有橫向指示線與 hover 高亮），支援上下自由調整範圍比例，以符合多種解析度的顯示器。
   * **檔案變更**：[Sidebar.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/Sidebar.tsx)、[Sidebar.module.css](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/Sidebar.module.css)。

2. **清空空白繪圖面板與移除冗餘初始化按鈕**
   * **說明**：完全精簡了空白繪圖框格的內容。移了原本陳列在空白面板上的 3D/HUD/地圖/FFT 等直接按鈕。取而代之的是，顯示一個乾淨精緻的虛線拖曳區域（帶有 Grid SVG 圖示與 `請從左側拖曳數據欄位或快捷工具至此顯示` 提示字樣），促使版面保持最乾淨的狀態。
   * **檔案變更**：[ChartPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/ChartPanel.tsx)。

3. **收納 3D / HUD / Map 至「🛠️ 互動工具箱」分類**
   * **說明**：在 `QuickPlotPresets.tsx` 快捷選單最下方新增了 `🛠️ Interactive Toolbox (互動工具箱)` 分類，將 `3D 姿態觀測器`、`AHRS 航空水平儀`、`2D 地圖軌跡` 三個互動式工具納入該快捷分類下，限制必須由左側選單拖曳至畫框內才能初始化使用。
   * **檔案變更**：[QuickPlotPresets.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/QuickPlotPresets.tsx)。

4. **單一繪圖框格的清空與重置行為**
   * **說明**：修正了 `appStore.tsx` 中 `REMOVE_PANEL` 的行為。當僅剩下 1 個 Panel 時，點擊面板右上角的 "X" 按鈕將會將該 Panel 的類型重新設回 `'empty'`，並清除所有已載入的 series 資料，將面板狀態成功重設回初始空白引導區塊。
   * **檔案變更**：[appStore.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/store/appStore.tsx)。

5. **PID 階躍響應包絡圖 (Step Response Envelope)**
   * **說明**：重構了 `PidResponsePanel.tsx`。將左側顯示區域分割為上下兩個圖表區：
     * **時域對齊比較圖**：顯示原始時間軸上的 setpoint 與 feedback 的對齊波形。
     * **正規化階躍響應包絡圖**：自動偵測期望值發生突變（階躍）的區間，提取出多個長度為 0.8s ~ 1.5s 的階躍區段並進行正規化對齊（歸一化幅值至 1.0），背景繪製所有個別階躍的淡色細線，並疊加一條亮藍色的平均響應包絡曲線。右側的指標報告（RMSE, R, Lag）與調校診斷建議予以保留。
   * **檔案變更**：[PidResponsePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/PidResponsePanel.tsx)、[PidResponsePanel.module.css](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/PidResponsePanel.module.css)。

6. **自動計算與本地記憶體快取 (FFT & PID)**
   * **說明**：移除了需要使用者點擊的「計算」按鈕。現在不管是 FFT 頻譜面板或 PID 對齊面板，在**面板掛載 (Mount)**、**分頁切換**以及**主圖表時間軸縮放 (Zoom/Pan)** 時都會自動觸發背景 Web Worker 計算。
   * **防抖與快取**：加入了 300ms 的防抖 (Debounce) 以免在快速縮放 timeline 時產生連續計算卡頓，並於元件內新增了 `fftCacheRef` 與 `pidCacheRef` 快取暫存。已計算過的區間將由快取立即回傳並秒級繪製。
   * **檔案變更**：[VibrationPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/VibrationPanel.tsx)、[PidResponsePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/PidResponsePanel.tsx)。

7. **多磁力計同步模長與 EKF GSF 航向比對 (相容 sensor_mag[n])**
   * **說明**：自動掃描 ULog Summary 中所有的 `vehicle_magnetometer` 或 **`sensor_mag`** 實例（例如 Compass 0, 1, 2...），相容 `magnetometer_ga` 以及舊款 `x`、`y`、`z` 欄位命名。藉由插值技術將其全部對齊到同一基準時間軸，並疊加於同一個 Vector Norm 圖表上對比。
   * **背景對照與 GSF**：
     * 右側控制台加入了「地磁場對照基準說明」，明記全球地磁場強度分佈（0.25G ~ 0.65G），提供使用者判斷是否羅盤受到配線大電流 EMI 干擾。
     * 讀取並插值對齊了 `yaw_estimator_status` 的無磁估計 (GSF Yaw) 角度以及 `sensor_gps` 的 COG 地面航向，於 Heading 圖表中同時重疊 EKF Yaw、GSF Yaw、GPS COG，並在控制台計算並提供平均偏差的偏角診斷。
   * **檔案變更**：[MagneticPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/MagneticPanel.tsx)、[MagneticPanel.module.css](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/MagneticPanel.module.css)。

8. **遙控器操縱桿與 Failsafe 圖表加載修復 (同時加載與快捷分頁比對)**
   * **說明**：修正了 `StatusModePanel.tsx` 的加載掛起問題。現在系統會在背景同時平行加載 `manual_control_setpoint`、`rc_channels` 與 `input_rc`（若存在於日誌中）。
   * **搖桿快捷切換分頁**：在操縱桿圖表右上角新增了快捷分頁切換器 `[Setpoint (期望值)]`、`[RC Channels (歸一化)]` 與 `[Raw RC (PWM us)]`，使用者可隨時點擊切換，極度方便對位比對。
   * **完整通道與 PWM 數值繪製**：切換至 `Raw RC (PWM us)` 時，系統會自動解算並繪製 **`input_rc.values[0..n]` 的所有可用通道**，並自動將 Y 軸範圍設為與遙控信號相符的 `850 ~ 2150 us` 區間，展示完整的開關與微調旋鈕變化。
   * **檔案變更**：[StatusModePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/StatusModePanel.tsx)、[StatusModePanel.module.css](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/StatusModePanel.module.css)。

9. **獨立的飛行模式與解鎖狀態時序圖 (獨立畫格且時間軸對齊)**
   * **說明**：將 Flight Mode 與 Arming State 獨立出一個格子畫圖，不再以背景色帶的形式與操縱桿共用。
   * **繪製細節**：新增了中間的 `Flight Mode & Arming History` 折線圖，將解鎖狀態與導航狀態（Nav State）繪製為階躍折線圖（Stepped Line），並支援雙 Y 軸（左側為 Arm/Disarmed，右側為對應的 PX4 模式字串標籤如 POSCTL, ALTCTL, RTL...），橫向時間軸與上方搖桿輸入及下方 Failsafe 圖表完全對齊。
   * **檔案變更**：[StatusModePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/StatusModePanel.tsx)。

10. **工具箱模組支援滑鼠滾輪縮放與橫軸同步 (Toolbox Zoom Sync & Wheel Zoom)**
    * **說明**：在 `VibrationPanel`、`PidResponsePanel`、`MagneticPanel` 與 `StatusModePanel` 中皆全面綁定了滑鼠滾輪 `'wheel'` 縮放事件。
    * **同步機制**：在 PID、磁力計、飛行狀態等包含複數圖表的模組中，我們使用了 `uPlot.sync` 共享縮放同步實例。當使用者在同一個模組的任一圖表上使用滾輪縮放、游標懸停或拖曳平移 (Pan) 時，其餘圖表皆會即時聯動更新，保證橫向時間軸精確對齊。
    * **檔案變更**：[PidResponsePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/PidResponsePanel.tsx)、[MagneticPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/MagneticPanel.tsx)、[StatusModePanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/StatusModePanel.tsx)、[VibrationPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/VibrationPanel.tsx)。

11. **新增磁力計原始三軸圖表與全面列出計算結果**
    * **說明**：為解決磁力計的進階除錯需求，對 `MagneticPanel.tsx` 進行了重大擴展。
    * **原始三軸圖表**：在左側欄新增了第三張圖表 `Raw 3-Axis Magnetic Values`，以 Gauss 為單位繪製 X、Y、Z 三軸的原始磁力波動，並且在右上角提供了下拉選單 (Dropdown Selector)，使用者可隨時切換要觀測那一個 Compass 實例的數值，該圖表亦同步橫向滾輪縮放。
    * **全磁力計評估清單**：右側評估報告改為**循環列出日誌中偵測到的所有指南針實例**，分別計算並列出其平均模長 (Avg Norm)、磁力振盪幅度 (Fluct) 與 EMI 警告狀態，讓動力干擾無所遁形。
    * **檔案變更**：[MagneticPanel.tsx](file:///home/kenny/Git_KennySpace/HTML_uLog_analyzer/src/components/MagneticPanel.tsx)。



